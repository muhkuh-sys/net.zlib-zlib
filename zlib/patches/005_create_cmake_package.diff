diff -uNr old/CMakeLists.txt new/CMakeLists.txt
--- old/CMakeLists.txt	2016-11-23 23:17:24.807086362 +0100
+++ new/CMakeLists.txt	2016-11-23 23:20:45.104273850 +0100
@@ -212,6 +212,7 @@
 
 if(NOT SKIP_INSTALL_LIBRARIES AND NOT SKIP_INSTALL_ALL )
     install(TARGETS zlibstatic
+        EXPORT net.zlib-zlib
         RUNTIME DESTINATION "${INSTALL_BIN_DIR}"
         ARCHIVE DESTINATION "${INSTALL_LIB_DIR}"
         LIBRARY DESTINATION "${INSTALL_LIB_DIR}" )
@@ -226,6 +227,10 @@
 #    install(FILES ${ZLIB_PC} DESTINATION "${INSTALL_PKGCONFIG_DIR}")
 #endif()
 
+install(EXPORT net.zlib-zlib DESTINATION cmake)
+
+export(TARGETS zlibstatic FILE net.zlib-zlib.cmake)
+
 #============================================================================
 # Example binaries
 #============================================================================
